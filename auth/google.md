# Google Sign-In setup

1) Go to Google Cloud Console → Credentials → Create OAuth Client ID (Web) using http://localhost as origin.
2) Get CLIENT_ID and integrate Google Identity Services button on header.
3) For this school project, we’re using a placeholder click that stores a demo user. Replace with real GIS as needed.
